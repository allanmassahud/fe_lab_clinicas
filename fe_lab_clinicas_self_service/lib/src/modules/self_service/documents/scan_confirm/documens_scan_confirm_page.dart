import 'package:flutter/material.dart';
    
class DocumensScanConfirmPage extends StatelessWidget {

  const DocumensScanConfirmPage({ super.key });
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Documents confirm'),
      ),
      body: Container(),
    );
  }
}